"""`/hooks/*` — unversioned integration receivers. `POST /hooks/livekit` is the call
reconciliation receiver; provider delivery events are ingested as deployment
configuration rather than as a product endpoint (api/02 §3).
"""
